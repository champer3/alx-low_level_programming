#include <stdio.c>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
char c;
for (c = 'a'; c <= 'z'; c++)
  /* Print lower case letter */
putchar(c);
putchar('\n');

return (0);
}
