#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{


for (ch = 'A'; ch <= 'Z'; ch++)
  /* Print lower case letter */
putchar(ch);
putchar('\n');

return (0);
}
